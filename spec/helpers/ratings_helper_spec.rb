require 'rails_helper'

RSpec.describe RatingsHelper, type: :helper do
  # let(:creator) { User.create!(provider: "spotify", uid: "dannymaher", nick_name: "Danny", spotify_id: "asdfhb") }
  # let(:rater) { User.create!(provider: "spotify", uid: "ashleyyang", nick_name: "Ash", spotify_id: "skdhfls") }

  # let(:rated_sound_trek) { SoundTrek.create!(description: "I love biking and listening to this", title: "Biking", trekker_id: creator.id, playlist: "skdhfls", latitude: 41.8762180, longitude: -87.6531470) }
  # let(:unrated_sound_trek) { SoundTrek.create!(description: "I love waking and listening to this", title: "Walking", trekker_id: creator.id, playlist: "skdhfls", latitude: 41.8762180, longitude: -87.6531470) }

  # let(:rating) { Rating.create!(sound_trek_id: rated_sound_trek.id, trekker_id: rater.id, stars: 5) }

  # let (:session) { {user_id: rater.id} }

  # describe "#rated_by_current_user?" do
  #   it "returns true if the current user has already rated a soundtrek" do
  #     expect(rated_by_current_user?(rated_sound_trek)).to eq true
  #   end

  #   it "returns false if the current user has already rated a soundtrek" do
  #     expect(rated_by_current_user?(unrated_sound_trek)).to eq false
  #   end
  # end
end
